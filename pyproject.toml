[project]
name = "movie-catalog"
version = "0.1.0"
description = "movie-catalog"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "redis[hiredis]>=6.4.0",
]
